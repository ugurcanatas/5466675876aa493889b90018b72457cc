import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

// const hotelModule = {
//   state: () => ({}),
// };

export default new Vuex.Store({
  state: {
    hotelList: [],
  },
  mutations: {},
  actions: {},
  modules: {},
});
